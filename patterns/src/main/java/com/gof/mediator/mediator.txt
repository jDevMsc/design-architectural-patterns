Mediator (Mediator)

Encapsulating the way objects interact

For what:
To identify an object that encapsulates a way of interacting with many objects and providing a weak connection between these objects

-connections between objects are complex and clearly defined
-You cannot reuse the object because it exchanges information with other users